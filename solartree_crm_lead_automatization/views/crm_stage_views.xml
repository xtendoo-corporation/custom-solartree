<odoo>
    <!-- HERENCIA DE LA VISTA SEARCH -->
    <record id="crm_lead_stage_search_inherit" model="ir.ui.view">
        <field name="name">Stage - Search</field>
        <field name="model">crm.stage</field>
        <field name="inherit_id" ref="crm.crm_lead_stage_search"/>
        <field name="arch" type="xml">
            <field name="team_id" position="after">
                <separator string="Requerimientos para cambiar de etapa"/>
                <field name="allowed_groups" widget="many2many_tags"/>
                <field name="user_offer_tot_required"/>
                <field name="user_id_required"/>
                <field name="margin_approval_required"/>
                <field name="assignation_solartree_code"/>
            </field>
        </field>
    </record>

    <!-- HERENCIA DE LA VISTA TREE -->
    <record id="crm_stage_tree_inherit" model="ir.ui.view">
        <field name="name">crm.stage.tree.inherit</field>
        <field name="model">crm.stage</field>
        <field name="inherit_id" ref="crm.crm_stage_tree"/>
        <field name="arch" type="xml">
            <field name="team_id" position="after">
                <field name="allowed_groups" widget="many2many_tags"/>
                <field name="user_offer_tot_required"/>
                <field name="user_id_required"/>
                <field name="margin_approval_required"/>
                <field name="assignation_solartree_code"/>
            </field>
        </field>
    </record>

    <!-- HERENCIA DE LA VISTA FORM -->
    <record id="crm_stage_form_inherit" model="ir.ui.view">
        <field name="name">crm.stage.form.inherit</field>
        <field name="model">crm.stage</field>
        <field name="inherit_id" ref="crm.crm_stage_form"/>
        <field name="arch" type="xml">
            <field name="team_id" position="after">
                <separator string="Requerimientos para cambiar de etapa"/>
                <field name="allowed_groups" widget="many2many_tags"/>
                <field name="user_offer_tot_required"/>
                <field name="user_id_required"/>
                <field name="margin_approval_required"/>
                <field name="assignation_solartree_code"/>
            </field>
        </field>
    </record>
</odoo>
