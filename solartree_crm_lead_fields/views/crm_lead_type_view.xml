<odoo>
    <record id="view_crm_lead_type_tree" model="ir.ui.view">
        <field name="name">crm.lead.type.tree</field>
        <field name="model">crm.lead.type</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="description"/>
            </tree>
        </field>
    </record>

    <record id="view_crm_lead_type_form" model="ir.ui.view">
        <field name="name">crm.lead.type.form</field>
        <field name="model">crm.lead.type</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="name"/>
                    <field name="description"/>
                </group>
            </form>
        </field>
    </record>

    <menuitem id="menu_crm_lead_type" name="Lead Types" parent="crm.crm_menu_root" sequence="10"/>

    <menuitem id="submenu_crm_lead_type" name="Lead Types" parent="menu_crm_lead_type" action="action_crm_lead_type"/>

    <record id="action_crm_lead_type" model="ir.actions.act_window">
        <field name="name">Lead Types</field>
        <field name="res_model">crm.lead.type</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
